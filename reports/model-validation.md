# Model Validation

Status: **engineering readiness verified; Phase 2 quality pending**  
Run status: `ready-for-phase2`  
Reason: the public multilingual checkpoint has no upstream ONNX export, but the fixed local exporter produced a parity-verified, versioned bundle. Publication, development quality interpretation, held-out evaluation, benchmark, and deployment remain outside this readiness handoff.

## Phase 1 English Baseline

The regression bundle is present locally at `models/laya` and is identified by:

- Repository: `receptron/laya-onnx`
- Revision: `68f27dfe5a27a54fb2b1fefc432f43f972e90868`
- Reference package: `@receptron/laya` `0.1.2`, git head `6478649e723122ca24bbf5fb69ed1010023c9754`
- Configuration: `max_len=512`, `head_max_len=192`
- Contract: five inputs (`input_ids`, `attention_mask`, `marker_pos`, `marker_mask`, `qtype`) and two outputs (`logits`, `act_probs`)
- Provenance: `test-data/reference-manifest.json`
- Regression entry points: `dotnet test LayaDotNetPoc.sln --no-restore`, `tools/run-acceptance.sh`, and `test-data/parity-fixtures.json`

The Phase 1 request contract remains `Choose the best transaction category`, `Is this transaction uncertain?`, the fixed 11-category order, and four state fields serialized in insertion order: `description`, `amount`, `currency`, `direction`.

## Multilingual Checkpoint Evidence

The Hugging Face API was queried on 2026-09-23:

- Repository: `convaiinnovations/laya-multilingual`
- Fixed observed repository commit: `052592a15d198d9ad47da779604259b10b47b7aa`
- Source: <https://huggingface.co/api/models/convaiinnovations/laya-multilingual>
- Available files: `model.safetensors`, `encoder/config.json`, `rl_agent_config.json`, `tokenizer/tokenizer.json`, `tokenizer/tokenizer_config.json`
- Missing upstream runtime export files: `laya.onnx` and verified external-data shards
- `rl_agent_config.json`: `max_len=1024`, `head_max_len=256`, `temperature=[1.0,1.0,1.0]`, `temperature_by_options={}`
- Tokenizer config: `<bos>`, `<eos>`, `<mask>`, `<pad>`, `<unk>`; model max length `8192`
- Encoder: `ModernBertForMaskedLM`, vocabulary size `256000`; this is not evidence of the Phase 1 Laya head or ONNX I/O contract

The observed upstream checkpoint is a Transformers/safetensors model rather than a published export. The local candidate was generated by the pinned Python exporter, parity-checked, and published as the verified multilingual bundle; it is not silently substituted for the English bundle. Its empty option calibration map and different max lengths are preserved. The official native source loaded all 170 state keys, the Python ORT candidate passed 20 fixtures, and .NET compatibility passed the same published bundle.

## Official Reference Candidate

The fixed model card links `https://github.com/NandhaKishorM/laya`. The PyPI `laya 0.3.7` artifact is published from source commit `010bacef009c855ccba814b51f7c8e1d38ab5e3f`.

- Wheel SHA-256: `370beee36a6962f0daed3f086146b7dc6cc825fee0a1941ec05ea7207989ea8a`
- Source distribution SHA-256: `cca6e999f18f8a5c5efb51d7f6870aebbfa95477855634d4ba933063c38d0c3f`
- Relevant load/inference paths: `laya.agent.Agent.__init__`, `laya.agent.Agent.system_one`, `laya.common.build_sequence`, `laya.common.DecisionModel.forward`, and `laya.common.collate_items`.
- Candidate status: source inspected, package installed in the isolated target toolchain, and fixed checkpoint loaded successfully by the native probe.
- Native evidence: `reports/multilingual-reference-probe.json`, five cases, six questions, maximum probability absolute error `0.00004968702268374689`.

## Blocked Dependencies

The following remain outside this readiness handoff and therefore remain blocked for full Phase 2:

- Real frozen-candidate policy selection and held-out quality
- Benchmark/concurrency and deployment validation
- Final Gate 3 owner criteria and decision

The engineering blocker was resolved by the fixed export/reference artifact and version-pinned publication process. Native reference evidence is recorded in `reports/multilingual-reference-validation.md`; it is combined with the published Python/.NET parity reports for readiness, not for Phase 2 quality certification.
